// Q6 (Medium): Write a function wordFrequency(str) that returns an object
// mapping each word (lowercase, punctuation stripped) to how many times it
// appears in the string.
// Example: wordFrequency("The cat sat. The CAT ran!") => {the:2, cat:2, sat:1, ran:1}

function wordFrequency(str) {
    // your code here
}

// ---- Run this file to check your answer ----
console.log(wordFrequency("The cat sat. The CAT ran!"));
// Expected Result: { the: 2, cat: 2, sat: 1, ran: 1 }
