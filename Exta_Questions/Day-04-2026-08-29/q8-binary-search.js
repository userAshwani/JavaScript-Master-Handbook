// Q8 (Medium): Write a function binarySearch(sortedArr, target) that returns
// the index of target in a sorted array, or -1 if not found. Must run in
// O(log n) time (no linear scan / indexOf).

function binarySearch(sortedArr, target) {
    // your code here
}

// ---- Run this file to check your answer ----
console.log(binarySearch([1, 3, 5, 7, 9, 11], 7));
// Expected Result: 3

console.log(binarySearch([1, 3, 5, 7, 9, 11], 4));
// Expected Result: -1
