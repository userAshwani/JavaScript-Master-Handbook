// Q5 (Medium): Write a function parseQueryString(query) that parses a URL
// query string (with or without leading "?") into a plain object.
// Example: parseQueryString("?name=Bob&age=30") => {name:"Bob", age:"30"}

function parseQueryString(query) {
    // your code here
}

// ---- Run this file to check your answer ----
console.log(parseQueryString("?name=Bob&age=30"));
// Expected Result: { name: 'Bob', age: '30' }

console.log(parseQueryString("city=Delhi"));
// Expected Result: { city: 'Delhi' }
